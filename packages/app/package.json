{
  "name": "@rayhealth/app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --composite false",
    "dev": "tsx watch src/server.ts",
    "lint": "eslint . --ext ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
